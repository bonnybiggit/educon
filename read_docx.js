const fs = require('fs');
const path = require('path');
const AdmZip = require('adm-zip'); // wait, I don't know if adm-zip is installed. Let's use standard library if possible, or install adm-zip

// Actually it's easier to just install adm-zip or mammoth
